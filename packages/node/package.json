{
  "name": "@le-space/node",
  "version": "0.1.35",
  "description": "Node and GitHub Actions adapters for shared Aleph tooling.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "dependencies": {
    "@le-space/core": "workspace:*",
    "@le-space/shared-types": "workspace:*",
    "@le-space/rootfs": "workspace:*",
    "ethers": "^6.15.0"
  },
  "scripts": {
    "build": "node --check ./src/index.ts && node --check ./src/env.ts && node --check ./src/github-outputs.ts && node --check ./src/signer.ts && node --check ./src/deploy-outputs.ts && node --check ./src/deploy-plan.ts && node --check ./src/deploy-executor.ts && node --check ./src/action-runner.ts && node --check ./src/rootfs-runner.ts",
    "build:publish": "tsup src/index.ts --format esm --dts --clean --tsconfig tsconfig.publish.json",
    "test": "node --test ./test/**/*.test.ts",
    "lint": "echo \"node lint placeholder\""
  }
}

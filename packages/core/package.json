{
  "name": "@shared-aleph/core",
  "version": "0.1.0",
  "description": "Reusable Aleph deployment, runtime, and guest lifecycle logic.",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "dependencies": {
    "@shared-aleph/shared-types": "workspace:*"
  },
  "scripts": {
    "build": "node --check ./src/index.ts && node --check ./src/manifests.ts && node --check ./src/crns.ts",
    "test": "node --test ./test/**/*.test.ts",
    "lint": "echo \"core lint placeholder\""
  }
}
